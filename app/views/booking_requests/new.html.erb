<% provide(:title, 'Booking') %>
<% provide(:button_text, 'Create my booking request') %>
<h1>booking</h1>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
  	  <% if @booking_request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@booking_request.errors.count, "error") %> prohibited this product from being saved:</h2>
      <ul>
      <% @booking_request.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <%= render 'form' %>
  </div>
</div>
